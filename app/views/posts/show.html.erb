<h1><%= @post.title %></h1>
<p><%= @post.content %></p>

<%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to 'Delete', post_path, :method => :delete, data:{ confirm: 'Are you ok with it?'} %>
<%= link_to 'Back', root_path %>